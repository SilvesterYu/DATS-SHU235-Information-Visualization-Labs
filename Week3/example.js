import {firstName, lastName, year} from './person.js';
// the above line. If not start with "./", person.js cannot be successfully imported

// note, in the below line, it is not a single quotation mark 
// but the little dot on the top-left of the keyboard!
console.log(`${firstName} ${lastName} was born in ${year}.`);

// to show the result, we need to create html file.

 